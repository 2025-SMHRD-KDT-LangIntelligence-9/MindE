# 루트 requirements.txt
# 백엔드(backend/requirements.txt)와는 별개로, 프로젝트 전반에서 공통으로 쓰는
# 개발/AI 관련 의존성을 모아두는 파일입니다. (스크립트 실행, 노트북 분석 등)
# TODO: 실제 사용하는 패키지로 정리

-r backend/requirements.txt

# AI / ML 공통
torch==2.2.0
transformers==4.38.0
pandas==2.2.0
numpy==1.26.4

# 개발용
pytest==8.0.0
black==24.1.1
